# ClipboardHub (Windows, C, TUI)

Native terminal clipboard manager (day-1 MVP).  
Build (MSYS2 MINGW64):

```bash
cmake --preset windows-release
cmake --build build/release
./build/release/chub --version
